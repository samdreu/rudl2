[package]
name = "copper-sim"
version = "0.1.0"
edition = "2024"
description = "Copper async simulation executor and the Verilator equivalence verifier"

[dependencies]
copper-core = { version = "0.1.0", path = "../copper-core" }
futures = { version = "0.3.32", default-features = false, features = ["executor"] }
log = "0.4.29"
syn = "2.0.114"
