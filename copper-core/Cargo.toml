[package]
name = "copper-core"
version = "0.1.0"
edition = "2024"
description = "Copper hardware types (Logic, Bits, Clock, ports, Memory), clock-domain machinery, and every IR in the pipeline"

[dependencies]

[dev-dependencies]
trybuild = "1.0"
